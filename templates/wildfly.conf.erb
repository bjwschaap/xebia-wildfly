# General configuration for the init.d scripts,
# not necessarily for JBoss AS itself.
# default location: /etc/default/wildfly

# Location of JDK	
# JAVA_HOME=/usr/lib/jvm/default-java

# Location of WildFly
JBOSS_HOME=<%= @install_dir %>/wildfly

# The username who should own the process.
JBOSS_USER=<%= @user %>

# The mode WildFly should start, standalone or domain
JBOSS_MODE=<%= @mode %>

# Configuration for standalone mode
#
JBOSS_CONFIG=<%= @profile %>.xml

# Configuration for domain mode
#
# JBOSS_DOMAIN_CONFIG=domain.xml
# JBOSS_HOST_CONFIG=host-master.xml

# The amount of time to wait for startup
STARTUP_WAIT=<%= @wait_time %>

# The amount of time to wait for shutdown
SHUTDOWN_WAIT=<%= @wait_time %>

# Location to keep the console log
JBOSS_CONSOLE_LOG=<%= @console_log %>

# Location to PID file
JBOSS_PIDFILE=<%= @pid_file %>

# Bind address (-b=<JBOSS_BIND_ADDRESS>)
JBOSS_BIND_ADDRESS=<%= @bind_address %>

# Bind address for management (-bmanagement=<JBOSS_BIND_ADDRESS_MANAGEMENT>)
JBOSS_BIND_ADDRESS_MANAGEMENT=<%= @bind_address_management %>
