    <socket-binding-group name="standard-sockets" default-interface="public" port-offset="${jboss.socket.binding.port-offset:0}">
        <socket-binding name="management-http" interface="management" port="<%= scope.lookupvar('wildfly::management_http_port')%>"/>
        <socket-binding name="management-https" interface="management" port="<%= scope.lookupvar('wildfly::management_https_port')%>"/>
        <socket-binding name="ajp" port="<%= scope.lookupvar('wildfly::ajp_port')%>"/>
        <socket-binding name="http" port="<%= scope.lookupvar('wildfly::http_port')%>"/>
        <socket-binding name="https" port="<%= scope.lookupvar('wildfly::https_port')%>"/>
        <% if scope.lookupvar('wildfly::profile') == "standalone-full" -%>
        <socket-binding name="jacorb" interface="unsecure" port="3528"/>
        <socket-binding name="jacorb-ssl" interface="unsecure" port="3529"/>
        <socket-binding name="messaging-group" port="0" multicast-address="${jboss.messaging.group.address:231.7.7.7}" multicast-port="${jboss.messaging.group.port:9876}"/>
        <% end -%>
        <socket-binding name="txn-recovery-environment" port="4712"/>
        <socket-binding name="txn-status-manager" port="4713"/>
        <outbound-socket-binding name="mail-smtp">
            <remote-destination host="localhost" port="25"/>
        </outbound-socket-binding>
    </socket-binding-group>